# Tulum Trip Companion 2026

Mobile-first React + TypeScript PWA for a Riviera Maya trip. Designed for iPhone Safari and GitHub Pages.

Run locally with `npm install && npm run dev`. Production build: `npm run build`.
